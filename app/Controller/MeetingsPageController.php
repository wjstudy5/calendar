<?php
class MeetingsPageController extends AppController {
	public function index() {
		// $events_rows ;
		// $events = array();
		// $nicknames = array();
		// foreach($events_rows as $row) {
		// 	$nickname = $row['nickname'];
		// 	if (!isset($events[$nickname])) {
		// 		$events[$nickname] = array();
		// 		array_push($nickname, $nicknames);
		// 	}
		// 	array_push($row, $events[$nickname]);
		// } 
	}
}
?>